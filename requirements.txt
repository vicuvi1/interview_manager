# Interview Manager — runtime + dev dependencies
fastapi>=0.110
uvicorn[standard]>=0.27
SQLAlchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2
python-multipart>=0.0.9
# zoneinfo database for Windows (no system tz DB there)
tzdata>=2024.1

# --- dev / test ---
httpx>=0.27
pytest>=8.0
