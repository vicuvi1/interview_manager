/** Account that is automatically treated as an admin (also enforced in SQL —
 *  see supabase/migrations/0005_auto_admin.sql). Keep the two in sync. */
export const ADMIN_EMAIL = "victorbarbuta54@gmail.com";
